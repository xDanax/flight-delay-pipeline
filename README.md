# flight-delay-pipeline
Automated Python pipeline that ingests raw U.S. flight delay data, applies cleaning and transformation logic, and loads analytics-ready tables into a relational database for downstream analysis
